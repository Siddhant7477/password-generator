A Simple command-line tool built in Python that generates strong,random passwords and checks their strength.

Features:

Generates a random password using letters,numbersand symbols.
Lets you choose how many letters, numbers and symbols to include.
Shuffles the characters so the password is truly random
Checks password strength and gives feedback(length,letters,symbols).
